name: Mentalist1

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print a custom message
      run: echo "This is my second pipeline running successfully!"
